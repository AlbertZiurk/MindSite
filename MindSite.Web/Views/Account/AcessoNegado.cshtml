@{
    ViewData["Title"] = "Acesso Negado";
    Layout = "_Layout";
}

<div class="min-h-[60vh] flex items-center justify-center">
    <div class="text-center px-6">
        <p class="text-8xl font-inria font-bold mb-4" style="color:var(--brown)">403</p>
        <h1 class="text-2xl font-bold mb-3" style="color:var(--text)">Acesso Negado</h1>
        <p class="mb-8 max-w-sm mx-auto" style="color:var(--text-muted)">Você não tem permissão para acessar esta página.</p>
        <a asp-controller="Home" asp-action="Index" class="btn-primary px-8 py-3">Voltar ao início</a>
    </div>
</div>
